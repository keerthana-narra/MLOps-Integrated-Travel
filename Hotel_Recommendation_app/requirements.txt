# scikit-surprise needs a C compiler to build from source via pip on Windows;
# prefer `conda install -c conda-forge scikit-surprise` there instead (see
# environment.yml at the repo root, which is the recommended setup path).
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.3.2
scikit-surprise==1.1.5
streamlit==1.37.1
gender-guesser==0.4.0
matplotlib==3.10.9
seaborn==0.13.2
joblib==1.5.3
